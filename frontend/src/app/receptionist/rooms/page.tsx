import ReceptionistRoomManagement from './RoomManagementClient';

export const metadata = {
  title: 'Room Management | Meseret Hotel',
};

export default function Page() {
  return <ReceptionistRoomManagement />;
}